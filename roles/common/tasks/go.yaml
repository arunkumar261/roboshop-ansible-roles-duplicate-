- name : install go
  ansible.builtin.package:
    name : golang
    state: present

- name : build go application
  ansible.builtin.command : "{{item}}"
  loop
  - go mod init dispatch
  - go set
  - go build
  args:
    chdir: /app